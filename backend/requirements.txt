# ============================================
# CipherPoint Backend — Production Dependencies
# ============================================
# Install:  pip install -r requirements.txt
# Tested with Python 3.11
# ============================================

# --- Web framework ---
fastapi==0.135.3
uvicorn[standard]==0.38.0
gunicorn==21.2.0

# --- Database / ORM ---
SQLAlchemy==2.0.48
# psycopg2-binary==2.9.9   # Uncomment only if using Postgres (not needed for Turso)

# --- Config / Secrets ---
python-dotenv==1.0.0
PyJWT==2.13.0
bcrypt==5.0.0

# --- HTTP client (Turso libSQL + Telegram Bot API) ---
requests==2.32.5

# --- Form / file uploads (for FastAPI UploadFile / File) ---
python-multipart==0.0.32
