# ============================================
# CipherPoint Backend — Production Dependencies
# ============================================
# Install:  pip install -r requirements.txt
# Tested with Python 3.11
# ============================================

# --- Web framework ---
fastapi==0.95.0
uvicorn[standard]==0.21.0
gunicorn==21.2.0
starlette==0.26.1
anyio==3.7.1   # Pinned: anyio 4.x breaks starlette 0.26 (to_thread API removed)

# --- Database / ORM ---
SQLAlchemy==2.0.52
# psycopg2-binary==2.9.9   # Uncomment only if using Postgres (not needed for Turso)

# --- Config / Secrets ---
python-dotenv==1.0.0
PyJWT==2.13.0
bcrypt==4.0.1

# --- HTTP client (Turso libSQL + Telegram Bot API) ---
requests==2.31.0

# --- Form / file uploads (for FastAPI UploadFile / File) ---
python-multipart==0.0.32
